{
  "name": "dog-shelter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Adoption and rescue platform for Wawitas Red de Apoyo, Cochabamba, Bolivia",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "emulators": "firebase emulators:start",
    "rules:test": "firebase emulators:exec --only firestore \"node --test test/rules/*.test.js\""
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "astro": "^7.1.6",
    "firebase": "^12.17.0"
  }
}
